import { AdminPortalPage } from '@/components/AventiWebApp';

export const metadata = {
  title: 'Admin — Aventi',
  description: 'Admin portal to manage users, settings and content',
};

export default function Admin() {
  return <AdminPortalPage />;
}
