{
  "name": "@aventi/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly",
    "lint": "echo 'No lint configured for @aventi/contracts yet'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'No tests for @aventi/contracts yet'"
  }
}
